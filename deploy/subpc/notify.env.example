# 実装セッションの状態報告（scripts/session-notify.sh）の設定（#1219）。
#
# 置き場所: ~/.config/issue-deck/notify.env
#   ISSUE_DECK_NOTIFY_ENV で別の場所を指定することもできる。
#
#   install -D -m 600 deploy/subpc/notify.env.example ~/.config/issue-deck/notify.env
#
# **このファイルが無くてもセッションはそのまま起動する。** ここにあるのは待ち時間の調整だけで、
# すべて省略可（既定値で回る）。
#
# **かつてはSignalyのwebhook URL（SESSION_NOTIFY_WEBHOOK_URL）もここに置いていたが、#2280で
# 削除した。** セッションの入力待ちはissue-deck自身のPush通知（`00.check-user`が付いたIssueを
# 鳴らす）で届くため、外部サービスへ送る経路は不要になった。既存のホストに残っている
# SESSION_NOTIFY_WEBHOOK_URL の行は、読む側が無くなったので消してよい。
#
# 報告先（APP_BASE_URL・DISPATCH_SECRET）はディスパッチpollerと同じ dispatch.env から読む。
# あちらはsystemdの EnvironmentFile= がpollerのプロセスへ渡すもので、こちらはフックの
# プロセスが自分で読む。読む主体も更新のタイミングも別なので、1つにまとめない。

# 計画を出したあと、issue-deckの画面からの返事を何秒まで待つか（#2061）。**省略可**（既定1800＝30分）。
#
# 待っている間は端末に承認プロンプトが出ず、画面（Issue詳細の「計画の承認を待っています」）
# からだけ承認・修正を送れる。待ち切ると従来どおりのプロンプトが端末に出るので、設定を
# 変えても壊れない。`0`にすると待たず、従来どおりの動きになる。
# 60〜3600の範囲へissue-deck側が丸める。
#SESSION_PLAN_WAIT_SECONDS=1800

# `AskUserQuestion`で聞いたあと、issue-deckの画面からの回答を何秒まで待つか（#2189）。
# **省略可**（既定300＝5分）。
#
# **計画の待ち時間（30分）よりわざと短くしてある。** 質問はturnの途中で何度も起きる常用経路で、
# 待っている間は端末に選択フォームが出ない。しかも`Esc`は待ちを抜けてフォームへ戻るのではなく
# **turnごと打ち切る**（結果が「User declined to answer questions」になる）ため、端末に
# 座っている人が長く待たされると作業のやり直しになる。
#
# 待ち切ると従来どおりのフォームが端末に出るので、長くしても短くしても壊れない。
# `0`にすると待たず、#2189より前の動き（端末とRemote Controlだけ）に戻る。
# 60〜3600の範囲へissue-deck側が丸める。スマホから答えることが多いなら長くしてよい。
#
# **引く間隔（SESSION_PLAN_POLL_INTERVAL_SECONDS）と降りるまでの猶予
# （SESSION_PLAN_POLL_GRACE_SECONDS）は計画と共有する。**
#SESSION_QUESTION_WAIT_SECONDS=300

# 待っている間にissue-deckへ届かない状態がここまで続いたら、待つのをやめる（秒。#2108）。
# **省略可**（既定60）。
#
# **1回の失敗では降りない。** 30分待つあいだに数百回引くため、瞬断や再起動で1回外すことは
# 普通に起きる。ここを`0`にすると1回でも失敗した時点で降り、画面にはカウントダウンが
# 残ったまま押しても届かないパネルになる（#2103で実際に起きた）。
#SESSION_PLAN_POLL_GRACE_SECONDS=60
